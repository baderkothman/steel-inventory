{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the steel inventory app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
